int multi[]={1,3,-3};

float a = sqrt (multi[0]);
float b = sqrt (multi[1]);
float c = sqrt (multi[2]);
float x = (1);
float buffer = (x^2 + 2*x -3);

float x1=((b * -1) + sqrt(b*b - (4*a*c)/2*a));
print ("x is"+x1);
